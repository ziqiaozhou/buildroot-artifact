name: Build Docker Images

on:
  push:
    branches:
      - artifact-snp
  workflow_dispatch:

jobs:
  artifact-build:
    runs-on: ubuntu-latest

    steps:
      - name: checkout source
        uses: actions/checkout@v2
        
      - name: build
        run: |
          cp config-snp-tiny .config
          sudo apt-get install libncurses-dev gawk flex bison openssl libssl-dev dkms libelf-dev libudev-dev libpci-dev libiberty-dev autoconf -y 
          make -j$(proc)
          
      - name: Release & Assets
        uses: Hs1r1us/Release-AIO@v1.0
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
        with:
          tag_name: x86_64
          # The name of the release
          release_name: initramfs.cpio
          # Text describing the contents of the tag
          body: x86 initramfs.cpio
          # The path to the asset you want to upload
          asset_files: output/images
          # `true` to create a draft (unpublished) release, `false` to create a published one. Default: `false`
          draft: true
